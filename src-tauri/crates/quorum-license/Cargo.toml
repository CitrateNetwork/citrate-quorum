# WP-S1.6 — the seat-metering seam. Quorum is priced per governed seat (Q20).
# S1 defines the trait + an honest no-op; S9 implements real metering + licensing.
[package]
name = "quorum-license"
version = "0.0.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "citrate-quorum seat-metering seam — the LicenseDomain trait + a no-op that never blocks and says so."

[lints]
workspace = true
