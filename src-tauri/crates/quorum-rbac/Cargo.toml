# WP-S1.5 — RBAC contract bindings, generated FRESH from citrate-chain ABIs.
# Never copied from the private citrate-boeing-shell bindings (Q2).
[package]
name = "quorum-rbac"
version = "0.0.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "citrate-quorum RBAC bindings — function selectors + event signatures for the six BFR-02 contracts, generated from chain ABIs."

[lints]
workspace = true
