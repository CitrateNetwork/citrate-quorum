# WP-S2 (integration) — on-chain clearance reads + the session resolver.
[package]
name = "quorum-clearance"
version = "0.0.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "citrate-quorum on-chain clearance reads (ClassificationRegistry + TenantHierarchy via eth_call) and the session resolver that produces an EffectiveGrant, fail-closed."

[dependencies]
citrate-core-kit = { path = "../../../../citrate-core/kit" }
quorum-rbac = { path = "../quorum-rbac" }
quorum-session = { path = "../quorum-session" }
quorum-tenancy = { path = "../quorum-tenancy" }
serde_json = "1"
hex = "0.4"

[lints]
workspace = true
